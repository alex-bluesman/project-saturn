#pragma once

#include <basetypes>

namespace saturn {
namespace device {

class UartDevice
{
public:
	virtual void Rx(uint8_t *buff, size_t len) = 0;
	virtual void Tx(uint8_t *buff, size_t len) = 0;
};

}; // namespace device
}; // namespace saturn
