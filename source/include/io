#pragma once

#include <basetypes>

namespace saturn {

static inline void mem_write8(uint64_t addr, uint8_t data)
{
	volatile uint8_t *paddr = (uint8_t *)addr;
	*paddr = data;
}

}; // namespace saturn
